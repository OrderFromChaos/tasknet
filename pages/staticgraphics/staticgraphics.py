# All of these should take X, Y as input

# TODO:
# 1. Border around a region (x0, y0, xF, yF)
    # Works with window size + .border(0) already, no need to implement
# 2. Calendar starting on a day of the week and running for X days

class displayMonth:
    def __init__(self, window):
        self.window = window
    # TODO