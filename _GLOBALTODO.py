# Support vim bindings globally
# Can curses handle modifier keys? e.g. ctrl+h
# when autoscheduling: prioritize by position swapping (similar to task list)
# change task color when they're already autoscheduled
# "o" for overwrite, makes tasks blank by default instead of edit at end
# "m" for meta, switch to meta task view
# select screen for contexts which fit into meta view (go here on first call)
# sorts for (original order), (due date), (name), etc.
